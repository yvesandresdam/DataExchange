import 'package:flutter/material.dart';

class Mycap extends StatelessWidget {
  const Mycap({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      width: 100,
      color: Colors.red,
      child: Placeholder()
    );
  }
}